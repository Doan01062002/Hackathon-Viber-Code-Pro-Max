/** Type dùng chung nhiều feature. Type riêng của một feature nằm trong feature đó. */

/** Trạng thái của một thao tác async — dùng cho hook fetch dữ liệu. */
export type AsyncStatus = "idle" | "loading" | "success" | "error";
