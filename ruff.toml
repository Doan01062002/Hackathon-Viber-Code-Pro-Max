target-version = "py311"
line-length = 120

[lint]
select = ["E", "F", "I", "N", "W", "UP"]
ignore = ["E501"]

[lint.isort]
# Hai package local của repo — nếu không khai báo, ruff tưởng là thư viện ngoài
known-first-party = ["ai", "backend"]

[format]
quote-style = "double"
indent-style = "space"
